# AI Agent Open-Source Contribution Workflow

This file has been split into narrower references so the skill stays focused on core agent PRs.

Use:

- `repository-pool.md` for the fixed repository pool and inspection hints.
- `core-agent-opportunity-rubric.md` to decide whether a candidate is truly core-agent work.
- `pr-workflow.md` for issue screening, PR collision checks, technical design, implementation guardrails, PR drafts, and interview narration.

Do not use broad GitHub repository discovery by default.